/*En un sistema de base n, se utilizan n dígitos únicos para representar números. Por ejemplo:

En el sistema binario (base 2), solo se utilizan los dígitos 0 y 1.
En el sistema octal (base 8), se utilizan los dígitos del 0 al 7.
En el sistema hexadecimal (base 16), se utilizan los dígitos del 0 al 9 y las letras A a F para representar valores del 10 al 15.*/

const { contadorBase } = require('./proy_modules/functions.js');

contadorBase(10); //base n//
